Current branch scope
- This file matches the current repository tree on branch `RF`.
- It documents the live firmware pair only:
  - `ESP_RF_Octopus`
  - `Marlin/marlin-2.1.2.6`
- Older variants that existed in previous branches or layouts are intentionally not listed here.

ESP_RF_Octopus
- Role:
  - ESP32 or ESP-12S access point (alternative controllers)
  - web UI host
  - UART bridge to Octopus / Marlin
  - direct 8-input RF/button receiver
- PlatformIO board:
  - `esp32dev` for ESP32
  - `esp12e` in environment `esp12s` for ESP-12S / ESP8266
- ESP-12S replacement wiring and upload procedure:
  - See `Docs/esp-controllers.md` and `ESP_RF_Octopus/src/board_config.h`.
  - The ESP32 pin numbers below do not apply to ESP-12S.
- USB serial monitor:
  - `115200`
- Wi-Fi AP:
  - SSID: `ESP_RF_Octopus`
  - password: `octopus123`
  - IP: `192.168.4.1`
  - max clients: `8`

ESP32 <-> Octopus wiring
- UART to Octopus:
  - ESP32 `GPIO16` (RX2) <- Octopus `PD8`
  - ESP32 `GPIO17` (TX2) -> Octopus `PD9`
  - Marlin side uses `SERIAL_PORT_2 3`
  - link baud: `115200`
  - on the Octopus board this is the WIFI/UART header serial pair used by the ESP bridge
- Reset line:
  - ESP32 `GPIO23` -> Octopus `RESET`
  - output mode: `OUTPUT_OPEN_DRAIN`
  - active level: low
  - reset pulse: `250 ms`
  - common GND between ESP32 and Octopus is required
  - do not drive the Octopus reset line high from the ESP32

ESP32 RF/button inputs
- All button inputs use `INPUT` and are active-high, with a defined LOW idle level.
- Mapping:
  - `GPIO14` -> `LIGHT_ON`
  - `GPIO32` -> `LIGHT_OFF`
  - `GPIO25` -> `POS1`
  - `GPIO33` -> `POS2`
  - `GPIO26` -> `RANDOM`
  - `GPIO13` -> `DIMMER`
  - `GPIO27` -> `POS3`
  - `GPIO21` -> `RESERVE`

ESP32 action mapping
- `LIGHT_ON` -> turn lamp on and start looped `M215 S1`
- `LIGHT_OFF` -> turn lamp off and stop motion with the existing stop / abort sequence (`M108`, `M215 X`, `M410`)
- `DIMMER` -> step `M355 P...` brightness while held
- `POS1` -> `M215 P1`
- `POS2` -> `M215 P2`
- `POS3` -> `M215 P3`
- `RANDOM` -> pick one configured random code and send `M215 S<code>`
  - the selected `M215 S<code>` loop is restarted forever until another command clears it
- `RESERVE` -> no action assigned yet

Lamp control
- `ESP_RF_Octopus` does not drive a local lamp GPIO.
- Lamp control is forwarded to Marlin with `M355`.
- The actual lamp power output is generated on the Octopus board.

Marlin / Octopus firmware
- Marlin project:
  - `Marlin/marlin-2.1.2.6`
- Board:
  - `BOARD_BTT_OCTOPUS_V1_1`
- Machine name:
  - `Hexapod Spider`
- Host USB serial:
  - `SERIAL_PORT -1`
  - baud `250000`
- ESP bridge serial:
  - `SERIAL_PORT_2 3`
  - baud `115200`
  - board pins `PD8` / `PD9`

Axis naming
- G-code axes exposed by this config:
  - `X Y Z A B C`
- Internally in Marlin:
  - `A` is axis `I`
  - `B` is axis `J`
  - `C` is axis `K`

Octopus motor socket mapping
- This branch uses explicit 6-axis pin assignments in `Configuration.h`.
- Axis to motor socket mapping:
  - `X` -> `MOTOR0`
  - `Y` -> `MOTOR1`
  - `Z` -> `MOTOR2`
  - `A` / `I` -> `MOTOR3`
  - `B` / `J` -> `MOTOR4`
  - `C` / `K` -> `MOTOR5`

Detailed stepper pins
- `X` / `MOTOR0`
  - STEP `PF13`
  - DIR `PF12`
  - EN `PF14`
  - CS `PC4`
- `Y` / `MOTOR1`
  - STEP `PG0`
  - DIR `PG1`
  - EN `PF15`
  - CS `PD11`
- `Z` / `MOTOR2`
  - STEP `PF11`
  - DIR `PG3`
  - EN `PG5`
  - CS `PC6`
- `A` / `I` / `MOTOR3`
  - STEP `PG4`
  - DIR `PC1`
  - EN `PA0`
  - CS `PC7`
- `B` / `J` / `MOTOR4`
  - STEP `PF9`
  - DIR `PF10`
  - EN `PG2`
  - CS `PF2`
- `C` / `K` / `MOTOR5`
  - STEP `PC13`
  - DIR `PF0`
  - EN `PF1`
  - CS `PE4`

Driver types and motion defaults
- Driver type on all 6 axes:
  - `TMC5160`
- RMS current on all 6 axes:
  - `2500 mA`
- Steps per unit:
  - `{ 8533.28, 8533.28, 8533.28, 8533.28, 8533.28, 8533.28 }` at 256 microsteps
  - Calibrated for the 3:1 gearing.
- Default max feedrate:
  - `{ 390, 390, 390, 390, 390, 390 }` mm/s
- Default max acceleration:
  - `{ 80, 80, 80, 80, 80, 80 }`
- Default normal acceleration:
  - `25`
- Homing feedrate:
  - `{ 320, 320, 320, 320, 320, 320 }` mm/min
- TMC chopper mode:
  - SpreadCycle on all 6 axes with the 24V timing profile.
- Software max position:
  - `120` on all six motion axes

Endstop mapping
- All active homing directions are to minimum:
  - `X_HOME_DIR -1`
  - `Y_HOME_DIR -1`
  - `Z_HOME_DIR -1`
  - `I_HOME_DIR -1`
  - `J_HOME_DIR -1`
  - `K_HOME_DIR -1`
- Enabled plugs:
  - `XMIN`
  - `YMIN`
  - `ZMIN`
  - `IMIN`
  - `JMIN`
  - `KMIN`
- Pin mapping:
  - `X_MIN_PIN` -> `PG6`  -> `X-STOP`
  - `Y_MIN_PIN` -> `PG9`  -> `Y-STOP`
  - `Z_MIN_PIN` -> `PG10` -> `Z-STOP`
  - `I_MIN_PIN` -> `PG11` -> `Z2-STOP`
  - `J_MIN_PIN` -> `PG12` -> `E0DET`
  - `K_MIN_PIN` -> `PG13` -> `E1DET`
- Extra spare inputs still defined in the config:
  - `U_MIN_PIN` -> `PG14` -> `E2DET`
  - `V_MIN_PIN` -> `PG15` -> `E3DET`

Lamp output on the Octopus
- Marlin enables `CASE_LIGHT_ENABLE`.
- `M355` drives:
  - `CASE_LIGHT_PIN PA1`
- On this board that is also:
  - `HEATER_BED_PIN PA1`
  - Octopus `BED` MOSFET output
- Current defaults:
  - case light default on: `true`
  - default brightness: `179`
- Important:
  - this configuration repurposes the bed output for lamps
  - there is no heated bed configured in this branch
  - `TEMP_SENSOR_BED` is `0`

Spider motion files and commands (ESP flash, when commanded through the ESP)
- Preset files:
  - `M215 H` -> direct `G28 X Y Z A B C` homing
  - `M215 P1` -> `/gcodes/pos1.txt`
  - `M215 P2` -> `/gcodes/pos2.txt`
  - `M215 P3` -> `/gcodes/pos3.txt`
- Random loop files:
  - `M215 S1` -> `/gcodes/input.txt`
  - `M215 S2` -> `/gcodes/input2.txt`
  - `M215 S3` -> `/gcodes/input3.txt`
  - `M215 S4` -> `/gcodes/input4.txt`
  - `M215 S5` -> `/gcodes/input5.txt`
  - `M215 S6` -> `/gcodes/input6.txt`
  - `M215 S7` -> `/gcodes/input7.txt`
- Runtime controls:
  - `M215 P` -> pause the ESP G-code stream
  - `M215 R` -> resume the ESP G-code stream
  - `M215 X` -> stop the ESP G-code stream

Current behavior notes
- On startup the ESP firmware waits for Marlin, then runs startup homing with `M215 H`.
- After successful startup homing it auto-starts `M215 S1`.
- Marlin controls lamp state through `M355`; the ESP stores motion programs and sends ordinary G-code to Marlin. Direct `M215` commands on the Octopus USB port still use the legacy SD-card implementation.
- `Docs/pinMapping` now intentionally follows the current branch layout, not the older multi-firmware tree.
